node_modules/
dist/
dist-model-smoke/
dist-model-benchmark/
coverage/
playwright-report/
test-results/
.superpowers/
.serena/
docs/
# Third-party model bundles must stay byte-exact (sha256-verified against upstream).
public/models/
# The frozen rebuild contract is a member of EVALUATOR_FILES: its BYTES are hashed
# into the evaluator digest, so exactly one tool may decide its shape. That tool is
# `JSON.stringify(canonical, null, 2)`, pinned byte-for-byte by
# benchmark/tests/rebuild-v3-policy.test.ts. Prettier inlines short arrays, which
# would rewrite the file (and move the digest) on every `npm run format`.
benchmark/rebuild-v3-policy.json
